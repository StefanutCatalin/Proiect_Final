@page
@model Proiect_Final.Pages.Structuri.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Structura[0].Angajat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Structura[0].Rezervare)
            </th>
            <th>
                @Html.DisplayNameFor(model =>
                model.Structura[0].Rezervare.Client)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Structura[0].Masa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Structura) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Angajat.NumeIntreg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rezervare.DataRezervare)
            </td>
                <td>
                    @Html.DisplayFor(modelItem =>
                item.Rezervare.Client.NumeIntreg)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Masa)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
